using UnityEngine;

[SerializeField]
public class TileEmptyStatus : MonoBehaviour
{
    public bool IsEmpty;

    // Start is called before the first frame update
    void Start()
    {
        IsEmpty = true;
    }
}
